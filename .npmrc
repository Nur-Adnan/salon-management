# Supply-chain hardening. pnpm reads minimumReleaseAge from pnpm-workspace.yaml;
# this min-release-age is the npm 11.10+ equivalent so the policy holds either way.
# Wait 7 days before resolving a freshly published version (typosquat/hijack window).
min-release-age=7
strict-peer-dependencies=false
